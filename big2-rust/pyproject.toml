[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "big2-rust"
version = "0.1.0"
requires-python = ">=3.10"
description = "Big 2 rules engine and vectorized environment core"

[tool.maturin]
features = ["python"]
module-name = "big2_rust"
